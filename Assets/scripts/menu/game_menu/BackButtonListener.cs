using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BackButtonListener : MonoBehaviour
{

	// Use this for initialization
	void Start ()
	{
		
	}
	
	//	// Update is called once per frame
	//	void Update ()
	//	{
	//		try {
	//			if (Input.GetKeyDown (KeyCode.Escape)) {
	//				if (gameObject.name.Contains ("Pause")) {
	//					if (gameObject.GetComponent<Settings> ().isMenuShowing)
	//						gameObject.GetComponent<Settings> ().hide ();
	//				} else {
	//
	//					if (gameObject.GetComponent<Help> ().isMenuShowing)
	//						gameObject.GetComponent<Help> ().hide ();
	//				}
	//			}
	//
	//		} catch (System.Exception e) {
	//			Debug.Log ("ExitGame.class Error");
	//		}
	//	}
}
